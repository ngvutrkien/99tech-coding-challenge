# Problem 4: Three ways to sum to n

### Requirements:

- Node.js

### Installation:

- `npm install`

### Usage:

- `npm run dev` to run the program in development mode.
- `npm run build` and `npm start` to build and run the program in production mode.